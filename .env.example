# GitHub Configuration
GITHUB_TOKEN=your_github_personal_access_token_here

# GitHub Projects v2 Configuration (optional - auto-discovers from repo)
# Only required if:
#   - Multiple projects are linked to the repo (for disambiguation)
#   - No projects are linked but you want to use an org project
# GITHUB_PROJECT_ID=PVT_kwDOBW_6Ns4BGTch
#
# Recommended: Create an "Assigned Instance" field (type: TEXT) in your project
# to track which LLM instance is working on each issue (e.g., "claude-1", "gemini-2")
# Configure it in .autonomous-config.json under project.fields.assignedInstance

# Optional: Claude API Key (if using Claude API instead of CLI)
# CLAUDE_API_KEY=your_claude_api_key_here

# Optional: Gemini API Key (future support)
# GEMINI_API_KEY=your_gemini_api_key_here

# Optional: OpenAI API Key for Codex (future support)
# OPENAI_API_KEY=your_openai_api_key_here
